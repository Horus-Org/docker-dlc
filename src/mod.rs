// In src/lib.rs or src/main.rs

mod DLCMessage; // Declare the DLCMessage module
mod DLCBuilder; // Declare the DLCBuilder module (if it's in a separate file)
mod Oracle;     // Declare the Oracle module (if it's in a separate file)
